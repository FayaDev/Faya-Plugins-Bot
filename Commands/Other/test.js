const { MessageEmbed } = require("discord.js");

module.exports = {
    name: "test",
    execute(client, message, args, Discord){
        try {    
        } catch (error) {
            console.log(error);
        }
    }
}